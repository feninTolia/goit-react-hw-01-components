import styled from 'styled-components';
import { color, typography, layout, space, shadow } from 'styled-system';

const Box = styled.div`
  ${color}
  ${typography}
  ${layout}
   ${space}
   ${shadow}
`;

export default Box;
